# JW Chat Agent POC Frontend

Open the deployed chat UI from the routed backend path:

```text
https://admin.dev.ai.jwhealthcare.com/jw-chat-agent
```

When served from a browser, the default backend base is the same origin plus
`/jw-chat-agent`.

To test another backend, append `?api=<base-url>` to the file URL.
