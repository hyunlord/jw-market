# DOC-4d 사용설명서 — 채팅 (유저용)

| 항목 | 값 |
|---|---|
| 기준 코드(develop) SHA | `9c34a7d5` (chat 코드 정본 커밋 `0900ed5e`, 경로 `chat/jw-chat-agent-poc/`) |
| 대상 화면 | 포탈 채팅 화면(SI 프론트) ↔ 채팅 서비스 `jw-chat-agent-poc`. 실동작 판정 기준 = **포탈 동등 경로(wf301 브리지 경유)** |
| 생성일 | 2026-07-18 |
| 문서 버전 | v1.0 |

> ★ 본 문서는 **현행 실동작**(운영 배포 기준)만 기술한다. "된다"는 서술은 포탈 경로(wf301) 실측 기준이다. 화면 UI 문구·레이아웃은 포탈(SI) 소관으로 확인 불가한 화면 세부는 `[확인 필요]`로 둔다.
> **★ 계보 각주.** 운영 채팅은 이미지 `da3fc15`(피처 브랜치, develop 미머지)로 돈다. 본 사용설명서는 **운영 실동작**을 기준으로 하며, develop 코드에는 아직 없는 운영 기능(딥리서치 `/deep`·엑셀 질답 SQL 경로)도 **운영에서 되므로** 안내에 포함한다. 코드 계보 상세는 DOC-1c 계보 각주 참조.

---

## 1. 채팅 사용법

- 포탈 채팅 화면에서 **질문을 입력**하면 답변이 마크다운(본문 + 필요 시 표·차트 + 출처)으로 표시된다. 응답 형식: 본문 `text`, 차트 `charts`, 출처 `sources`, 파일 그라운딩 시 `file_sources`(DOC-3b §1.3).
- 스트리밍 화면은 단계 진행(step)→본문(delta)→표(markdown_block)→차트→타이밍→완료 순으로 점진 표시된다(SSE, DOC-3b §1.3).
- **동시 사용자가 많으면** "현재 사용자가 많습니다. 잠시 후 다시 시도해주세요."가 표시될 수 있다(동시 처리 상한, 최대 대기 약 10초 후 안내; DOC-3b §3.1). 잠시 후 재시도하면 된다.
- 화면 진입 경로·버튼·대화 목록 UI 등 포탈 화면 세부: `[확인 필요]`(SI 포탈 프론트 소관). `[화면: 포탈 채팅 입력/응답 화면]`

## 2. 질문 유형별 안내 (잘 되는 것)

현행 라우팅이 실제로 처리하는 유형:

- **시장/브랜드 지표 조회**: 특정 브랜드·시장의 지표를 물으면 시장분석 데이터에 근거해 답한다. 답변에는 출처·표·차트가 붙는다. (라우팅·근거 조립: DOC-1c §1)
- **뉴스/이슈 기반 심층 설명("심층분석")**: 브랜드 관련 뉴스·이슈를 큐레이션 코퍼스에서 찾아 설명한다. 질문 내용으로 발동한다(DOC-3b §1.1).
- **딥리서치 — 질문 앞에 `/deep`**: 질문 맨 앞에 `/deep `를 붙이면(예: `/deep 리바로 시장 최근 동향`) 딥리서치 모드가 발동해 여러 자료를 병렬로 모아 **보고서형(핵심 요약·종합 분석)** 답변을 준다. 일반 답변보다 오래 걸린다(합성 예산 약 300초). 딥리서치의 웹 결과는 §3의 "미검증" 규칙을 따른다.
- **업로드 파일에 대한 질문(엑셀 포함)**: 파일을 올린 뒤 물으면 파일 근거로 답한다. **엑셀(XLSX)·CSV 같은 표 파일도 질답할 수 있다**(§4).
- **외부 근거가 필요한 질문(임상시험·허가·FDA·HIRA)**: 외부 조회로 보강하되, 외부 결과의 취급은 §3의 한계를 따른다.

> 질문은 **한 번에 완결된 문장**으로 쓰는 것을 권장한다(사유: §3의 멀티턴 한계).

## 3. 한계 (대답 못 하는 유형 · 주의) — 현행 실동작 기준

- **멀티턴 문맥 승계 — 미지원.** 이전 질문/답변은 저장은 되지만 다음 답변의 근거로 **자동 반영되지 않는다**(턴이 프롬프트에 주입되지 않음; `conversation.py` turns는 기록만). 따라서 "그럼 그건?"처럼 앞 답변에 의존하는 후속 질문은 의도대로 동작하지 않을 수 있다 → **각 질문을 완전한 문장으로**. (예외: 직전 턴이 "시장 기준 선택" 요청이면 그 선택만 이어짐.)
- **웹 검색 결과 = 미검증.** 실시간 웹 결과는 "웹 검색 결과(미검증)"로 **출처만 분리 표시**되고 내부 확정 근거로 승격되지 않는다(`client.py:730-731`). 웹발 수치는 참고로만.
- **외부 API(임상시험·허가·FDA·HIRA) 의존.** 해당 근거가 필요한 질문은 외부 조회에 의존하며, 실패 시 오류를 감추지 않고 부분 근거로 처리한다(임상시험은 실패 시 fail-closed로 해당 근거 없이 진행; DOC-3b §3.2). 외부 지연 시 답변이 느려지거나 그 근거가 빠질 수 있다.
- **미지원 브랜드/범위.** 시장분석 데이터에 없는 브랜드·범위는 답할 수 없다(근거 부재 시 확정 fact만 조립하는 폴백; DOC-3b §3). 없는 것을 지어내지 않는다.
- **파일 질답 범위 — 개선 중(현행 실측 기준).** 엑셀/CSV 표 파일 질답은 운영에서 **된다**(§4, 235 SQL 경로). 다만 형식별·시트 구성별 동작 범위는 계속 확장 중이며 전 형식·전 케이스가 동일하게 동작한다고 보장하지 않는다(지원 형식 전수는 전처리기(235) 소관 → 확인 필요). 대용량·복잡 시트는 준비 시간이 걸릴 수 있다(§4). 대규모 정형 분석은 시장분석 화면(DOC-4a)이 여전히 정본 경로다.
  - (참고) 표 파일을 채팅에 붙여도 되는 이유: 파일은 235 브리지가 처리한다. 채팅 자체의 텍스트 첨부 경로(로컬 RAG)는 표 파일을 받지 않지만, 그 경로는 실운영 파일 질답 경로가 아니다.

## 4. 파일 업로드 질답 사용법

- **방법**: 포탈에서 파일을 업로드한 뒤 그 파일에 대해 질문하면, 채팅이 파일 검색 결과를 근거로 답한다. 파일 근거가 쓰였으면 답변에 `file_sources`(파일명)가 표시된다.
- **처리 경로**: 파일 파싱·색인은 채팅 서비스가 직접 하지 않고 **wf301 브리지(`code-serving-235`)/전처리기**가 담당한다(`file_search_client.py:18-68`). 채팅은 그 결과 컨텍스트만 사용한다.
- **준비 시간(대형 파일 주의)**: 업로드 직후에는 색인이 끝나지 않았을 수 있다. 채팅의 파일 검색은 짧은 타임아웃(기본 3초, `file_search_client.py:29`)으로 조회하며, 준비 전이면 **파일 없이 답이 진행**된다. → 대형 파일은 잠시 기다린 뒤 질문하고, 파일 근거가 보이지 않으면 재질문한다.
- **지원 형식·범위**: 운영 235 브리지는 **엑셀(XLSX)·CSV 정형 파일을 표/집계로 질답**(SQL 경로)하고, PDF 등 비정형은 임베딩/문서이해로 처리한다(`[운영 이미지 기준]` da3fc15, DOC-1c §4). 즉 정형·비정형 모두 지원되나, **형식별·시트 구성별 상세 동작은 계속 확장 중**이라 전 케이스 동일 동작을 보장하지 않는다 → 형식 전수·한계는 235 소관 **[확인 필요]**. "전부 지원"으로 단정하지 않는다.
- **질문 없이 파일만** 올리면 "파일 저장 완료" 류 확인 답변이 온다(`sources=["file_upload"]`, app.py:476-492). 이후 질문하면 그 파일 맥락으로 답한다.

---

## [확인 필요] 목록
1. 포탈 채팅 화면 UI 세부(진입·대화 목록·버튼 문구) — SI 포탈 프론트 소관.
2. 파일 질답 **지원 형식 전수·형식별 한계** — 전처리기(code-serving-235) 소관, 채팅 repo 밖.
3. 대형 파일 색인 완료까지 실제 소요 시간(형식·크기별) — 실측치 필요(코드 상수 아님).
4. 딥리서치 답변의 체감 소요 시간 — 예산은 약 300초(DOC-3b §3.1)이나 체감 측정치는 별도.
5. **계보**: 딥리서치·엑셀 SQL 질답은 운영 이미지(da3fc15) 기능이며 develop 미머지 — 문서-운영 정본 일치는 PL 별건(DOC-1c 참조).

## 스크린샷 캡처 리스트
- `[화면: 포탈 채팅 입력/응답 화면]`
- `[화면: 파일 업로드 후 질답(파일 출처 표시)]`
- `[화면: 동시 사용자 많음 안내]`
