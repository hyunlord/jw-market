# DOC-1c 개발 문서 — 채팅 에이전트

> **스켈레톤 (골격). 담당: jw chat 세션 · 상태: ⏳ 기고 대기.**
> 작성 전 [README §3 작성 규칙](README.md) 필독. 근거 병기·추측 금지.
> 확인 불가는 `[확인 필요]` 표기 후 말미 목록.

| 항목 | 값 |
|---|---|
| 기준 코드(develop) SHA | `[기고 필요]` |
| 운영 리소스 | `[기고 필요: deployment jw-chat-agent-poc·serving·이미지 등 실측]` |
| 생성일 | `[기고 필요]` |
| 문서 버전 | v0.1 (스켈레톤) |

---

## 1. 채팅 에이전트 아키텍처 (질의 처리 흐름)

`[기고 필요]` 질의 수신 → 라우팅/플래너 → 툴/쿼리 → 답변 생성까지의 처리 흐름. 근거: `jw-chat-agent-poc/` 실코드 `파일:줄`. 일반 질의 vs 딥리서치 경로 구분.

## 2. wf301 브리지 · 포탈 연동 경로

`[기고 필요]` 포탈 → wf301 → 채팅 에이전트(`/chat/answer`) 호출 계약과 세션/로그 저장 경로. 근거: 브리지 rev_step·실측 호출. (계약 상세는 DOC-3b 참조 링크)

## 3. GenOS · Flowise 연동 (serving · 모델)

`[기고 필요]` serving 리소스·모델(예: gemini 계열)·라우팅 메커니즘. 근거: serving 배포·실측 리비전. 모델명/엔드포인트는 실값, 자격증명 값은 금지.

## 4. 파일 질답 경로

`[기고 필요]` 파일 업로드 → 임베딩/RAG → 질답 경로(엑셀/PDF 처리 포함). 근거: 파일 파이프라인 코드·preprocessor 연동.

## 5. 배포 형태 · 이미지

`[기고 필요]` deployment·replicas·HPA·이미지 태그/digest·빌드/배포 절차. 근거: 실측 매니페스트.

---

## [확인 필요] 목록
`[기고 필요]`

## 스크린샷/다이어그램 캡처 리스트
`[기고 필요]`
