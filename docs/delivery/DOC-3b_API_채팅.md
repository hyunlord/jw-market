# DOC-3b API 문서 — 채팅

> **스켈레톤 (골격). 담당: jw chat 세션 · 상태: ⏳ 기고 대기.**
> 작성 전 [README §3 작성 규칙](README.md) 필독. ★EP·파라미터·응답은 실코드·실호출 기반.
> 예시 응답은 실호출 캡처만(창작 금지). 확인 불가는 `[확인 필요]`.

| 항목 | 값 |
|---|---|
| 기준 코드(develop) SHA | `[기고 필요]` |
| 대상 서비스 | `[기고 필요: jw-chat-agent-poc svc·포트 등 실측]` |
| 실호출 캡처일 | `[기고 필요]` |
| 문서 버전 | v0.1 (스켈레톤) |

> 형식 참고: 기존 `DOC-3_API_명세서.md`(EP·메서드·파라미터표·응답 구조·실응답 예시 1건).

---

## 1. 채팅 EP 명세

`[기고 필요]` `/chat/answer` 등 채팅 EP 전수: 메서드·경로·핸들러(`파일:줄`)·파라미터(타입·필수·기본값)·응답 구조·실호출 예시 1건(캡처 경로 명기). 스트리밍(SSE) 여부 포함.

## 2. 포탈 ↔ 채팅 ↔ 백엔드 호출 계약

`[기고 필요]` 포탈(BFF) → wf301 → 채팅 에이전트 → (시장분석 백엔드/파일) 호출 계약. 헤더·세션 식별자·payload 형식. 근거: 실측 호출·브리지 코드.

## 3. 오류 응답 · 한계

`[기고 필요]` 오류 코드/메시지(실측), 타임아웃·크기 한계, 실패 시 동작. 근거: 실코드·실호출.

---

## [확인 필요] 목록
`[기고 필요]`
