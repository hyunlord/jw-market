apiVersion: batch/v1
kind: Job
metadata:
  name: jw-agent3-full-manual
  namespace: llmops
  labels:
    app: jw-agent3
    component: brand-strength
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 14400
  template:
    metadata:
      labels:
        app: jw-agent3
        component: brand-strength
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      restartPolicy: Never
      nodeSelector:
        genos: "enabled"
      containers:
        - name: agent3
          image: asia-northeast3-docker.pkg.dev/prj-jw-agn-stg-ai/ar-jw-agn-stg-genos-dev-01/jw-market-backend-api:v0.9.63-agent3-15bface5-20260706
          imagePullPolicy: Always
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
          env:
            - name: AGENT3_DB_HOST
              value: llmops-mariadb-service.llmops.svc.cluster.local
            - name: AGENT3_DB_PORT
              value: "3306"
            - name: D2_WRITER_USER
              valueFrom:
                secretKeyRef:
                  name: jw-mart-d2-writer
                  key: username
            - name: D2_WRITER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: jw-mart-d2-writer
                  key: password
            - name: AGENT3_DB_USER
              valueFrom:
                secretKeyRef:
                  name: jw-mart-d2-writer
                  key: username
            - name: AGENT3_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: jw-mart-d2-writer
                  key: password
            - name: AGENT3_DB_NAME
              value: jw_mart_d2_stage_20260630_r2
            - name: WF316_DIRECT_RUN_URL
              value: http://workflow-316.llmops.svc.cluster.local:8080/run/v2
            - name: AGENT3_WORKFLOW_REV
              value: "5365"
            - name: AGENT3_BRAND_SOURCE
              value: general_all
            - name: AGENT3_MODE
              value: dry-run
            - name: AGENT3_CHUNK_INDEX
              value: "0"
            - name: AGENT3_CHUNK_SIZE
              value: "500"
            - name: AGENT3_OUTPUT
              value: /tmp/agent3_full_result.json
          command: ["/bin/bash", "-lc"]
          args:
            - |
              set -euo pipefail
              cd /app
              python -m pipeline.scripts.agent3.run_source \
                --brand-source "${AGENT3_BRAND_SOURCE}" \
                --mode full \
                --source all \
                --output "${AGENT3_OUTPUT}"
              cat "${AGENT3_OUTPUT}"
